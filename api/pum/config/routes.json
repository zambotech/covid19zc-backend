{
  "routes": [
    {
      "method": "GET",
      "path": "/pums",
      "handler": "pum.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/pums/count",
      "handler": "pum.count",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/pums/:id",
      "handler": "pum.findOne",
      "config": {
        "policies": []
      }
    },
    {
      "method": "POST",
      "path": "/pums",
      "handler": "pum.create",
      "config": {
        "policies": []
      }
    },
    {
      "method": "PUT",
      "path": "/pums/:id",
      "handler": "pum.update",
      "config": {
        "policies": []
      }
    },
    {
      "method": "DELETE",
      "path": "/pums/:id",
      "handler": "pum.delete",
      "config": {
        "policies": []
      }
    }
  ]
}
