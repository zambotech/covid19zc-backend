{
    "routes": [
        {
            "method": "GET",
            "path": "/figures",
            "handler": "figures.index",
            "config": {
                "policies": []
            }
        }
    ]
}